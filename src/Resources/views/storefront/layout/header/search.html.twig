{% sw_extends '@Storefront/storefront/layout/header/search.html.twig' %}

{# shopware-block: 774b616ac8ff8c8770c4b0d13409fee12ba55226bd5fcbdd61c07e0d297d0cc0@v6.6.9.0 #}
{% block layout_header_search_input %}
    {% set extensionName = constant('NuonicSearchHub\\Extension\\SearchHubPageExtension::EXTENSION_NAME') %}

    {% if page.hasExtension(extensionName) %}
        {% block layout_header_searchhub_input %}
            <input type="search"
                   id="header-main-search-input"
                   name="search"
                   class="form-control header-search-input"
                   autocomplete="off"
                   autocapitalize="off"
                   placeholder="{{ 'header.searchPlaceholder'|trans|striptags }}"
                   aria-label="{{ 'header.searchPlaceholder'|trans|striptags }}"
                   value="{{ page.searchTerm }}"
                   data-track-id="searchBox">
        {% endblock %}
        {% else %}
            {{ parent() }}
    {% endif %}
{% endblock %}

{# shopware-block: fb98468b2b37df27bfdf674df9bb3e591961247b38f8030fe699b6bd26b9e1e3@v6.6.9.0 #}
{% block layout_header_search_button %}
    {% set extensionName = constant('NuonicSearchHub\\Extension\\SearchHubPageExtension::EXTENSION_NAME') %}

    {% if page.hasExtension(extensionName) %}
        {% block layout_header_searchhub_button %}
            <button type="submit"
                    class="btn header-search-btn"
                    aria-label="{{ 'header.searchButton'|trans|striptags }}"
                    data-track-id="searchButton">
                <span class="header-search-icon">
                    {% sw_icon 'search' %}
                </span>
            </button>
        {% endblock %}
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}
